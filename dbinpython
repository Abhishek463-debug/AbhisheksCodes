import mysql.connector
dbforcustomers = mysql.connector.connect(
  host = "localhost",
  user = "abhishek",
  password = "password"
  )
  cursorfordb = dbforcustomers.cursor()
  cursorfordb.execute("CREATE DATABASE customers_DB")
